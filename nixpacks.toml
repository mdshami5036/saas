[providers]
name = ["node"]

[phases.setup]
nixPkgs = ["nodejs_18", "npm-9_x"]

[phases.install]
cmds = ["cd backend && npm install"]

[phases.build]
cmds = ["cd backend && npx prisma generate && npx prisma db push --schema=prisma/schema.prisma"]

[start]
cmd = "node backend/src/index.js"
