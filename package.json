{
  "name": "autoprint-saas",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd backend && npm install",
    "postinstall": "cd backend && npx prisma generate",
    "start": "cd backend && npm start"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
